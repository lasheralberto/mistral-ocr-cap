export const processingTypeEnum = Object.freeze({
    LOCAL:'local',
    URL:'url',
    BASE64:'base64'
})

export const typeDocumentEnum = Object.freeze({
    IMAGE_URL:'image_url',
    DOCUMENT_URL:'document_url'
})

